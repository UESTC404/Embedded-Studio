media:
  - name: images
    label: 图片库
    input: public/images
    output: /images
    rename: safe
    categories: [image]
  - name: documents
    label: 文件库
    input: public/files
    output: /files
    rename: safe
    categories: [document]

content:
  - name: docs
    label: 网站文档（文件名即子链接）
    type: collection
    path: content/docs
    format: yaml-frontmatter
    exclude: [meta.json]
    filename:
      template: "{primary}.md"
      field: true
    view:
      fields: [title, description]
      primary: title
      sort: [title]
    operations:
      create: true
      rename: true
      delete: true
    fields:
      - name: id
        label: 文档内部 ID
        type: uuid
        required: true
        hidden: true
        options:
          editable: false
          generate: false
      - name: title
        label: 页面标题
        type: string
        required: true
        description: 网页正文和左侧导航显示的名称；修改后所有显示位置会自动同步。
      - name: description
        label: 页面简介
        type: text
        required: true
      - name: body
        label: 页面正文
        type: rich-text
        description: 图片请先上传到“图片库”，再从图片库插入；不要直接粘贴浏览器临时预览图。连续插入两张及以上图片会自动排成两列画廊，手机端自动变为单列，点击图片可查看原图。单张图片可在源码模式给地址末尾添加 #small、#medium、#large 或 #wide 调整尺寸；PDF 等附件请先上传到“文件库”，再于源码模式插入文件链接。
        options:
          format: markdown
          media: images
          switcher: true
          categories: [image]
          rename: safe

  - name: navigation
    label: 左侧导航
    type: file
    path: content/navigation.json
    format: json
    fields:
      - name: title
        label: 导航总标题
        type: string
        required: true
      - name: items
        label: 页面顺序与分类
        type: block
        list: true
        required: true
        description: 拖动条目可调整顺序。文档项会自动显示网站文档的真实标题；标题内容请回到对应文档中修改。
        blockKey: type
        blocks:
          - name: page
            label: 文档
            fields:
              - name: page
                label: 选择文档
                type: reference
                required: true
                options:
                  collection: docs
                  search: title,description
                  value: "{fields.id}"
                  label: "{primary}"
          - name: category
            label: 分类标题
            fields:
              - name: label
                label: 分类名称
                type: string
                required: true
          - name: unlisted
            label: 自动加入未列出页面
            fields:
              - name: note
                label: false
                type: string
                hidden: true

settings:
  content:
    merge: true
  commit:
    identity: user
    templates:
      update: "content: 更新 {path}"
